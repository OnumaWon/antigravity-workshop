{
  "name": "antigravity-workshop",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": "prisma generate --schema=./server/prisma/schema.prisma"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "@prisma/client": "^5.12.1",
    "prisma": "^5.12.1"
  }
}
